import 'package:flutter/material.dart';

class BookingTabletViews extends StatelessWidget {
  const BookingTabletViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Tablet view"),);
  }
}
